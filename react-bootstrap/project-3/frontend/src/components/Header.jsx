// src/components/Header.jsx
import React from 'react';

const Header = () => {
  return (
    <header className="text-center my-5">
      <h1>React + Flask + Vite + Bootstrap</h1>
    </header>
  );
};

export default Header;